a. The source code: code.cpp
b. A shell/batch script : test.bat(You can run it by just double-click on this file)
c. Text files with your test data: There two .adi fie(a.adi and b.adi),a .csv file(c.csv)
To run the progarm,you should compile the program into an executable file at the command line, and then enter valid arguments at the command line. (See test.bat for the format required by the question.)
## may be some bugs here
